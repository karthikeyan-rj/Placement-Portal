VITE_API_BASE_URL=http://localhost:8080/api
# Frontend-only Developer Mode. Set to true to show the Developer preview menu
# (no backend auth is bypassed). Keep false/absent for normal behavior.
VITE_DEV_MODE=false
